import { DataTypes } from './Types'

// const protocol = 'http'
// const hostname = 'localhost'
// const port = 3500

export const RestUrls = {
  // [DataTypes.PROJECTS]: `${protocol}://${hostname}:${port}/api/projects`
  [DataTypes.PROJECTS]: `/api/projects`
}

export const GraphQlUrl = '/graphql'